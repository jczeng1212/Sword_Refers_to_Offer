#include<vector>
using namespace std;

//先用快速排序对数组进行排序，然后将从k+1到input.size()部分进行erase()，返回剩下的部分
class Solution {
public:
    vector<int> GetLeastNumbers_Solution(vector<int> input, int k) {
        
    }
};